## テンプレ
## 作業記録 [M/D(n)]

### 1. 調査した内容
- 
- 

### 2. 悩み・疑問
- 
- 

### 3. 学習した内容
- 
- 

### 4. 設計
- 
- 

### 5. 仕様
- 
-



## 振り返り(9/8)
* **進捗**
    * トップ画面
    * 施設一覧表示
    * 施設詳細画面
    * 訪問ログ追加画面
* **調査した内容**
* **悩み疑問,課題**
    * 現状、トップ画面のどの都道府県を選択しても施設が一覧表示される。
    * テストユーザーは登録されているユーザーからランダムに選択されている。
    * 遷移機能しか追加されていないページがある
    * 施設の初期データが二件しかない
* **学習した内容**
    * ページ遷移の仕方
    * ページ遷移図の書き方
    * urlパラメータが文字列型になること
* **設計**
* **仕様**

## 作業記録 [9/9(1)]

### 1. 調査した内容
- urlパスパラメータ経由で渡されるidが常にstring型になるため、数値型intへキャストする挙動の確認。
- 

### 2. 悩み・疑問
- 新規施設を登録する際にフォームで都道府県を入力させるべきか、47択から選択するようにするか悩み中

### 3. 学習した内容
- 画面遷移の仕方
- 保存データの参照の仕方

### 4. 設計
- 遷移図に基づき、最小限機能を確保した画面を構築

### 5. 仕様
- **訪問ログ登録機能:** 訪問日・評価・コメントを入力して送信するとVisitテーブルへ保存されログ登録完了画面へ遷移する。
- **新規施設登録機能:** 施設名・都道府県を入力して送信するとFacilityテーブルへ保存され施設登録完了画面へ遷移する。

## 作業記録 [9/9(2)]

### 1. 調査した内容
- パスパラメータとクエリパラメーターの違い
- クエリパラメーターの場合のページ遷移方法
- 
### 2. 悩み・疑問
- クエリパラメーターとパスパラメーターの用途の使い分けがよくわからない
- ->個人的な理解としては、パスパラメーターは何かに定まるページに遷移するときに使う
- ->クエリパラメーターは今回のように都道府県というカテゴリで指定して遷移する場合に用いる

### 3. 学習した内容
- パスパラメータとクエリパラメーターの違い
- クエリパラメーターの場合のページ遷移方法

### 4. 設計
- facilitiesテーブルに含まれていた都道府県カラムを新しいテーブルとして切り出した
- 今回のアプリでは都道府県別で施設検索できる点を重視したため、新しくテーブルを作成することにした

### 5. 仕様
- Topページの都道府県を選択するとその県で登録済みの施設のみを一覧表示できるようにした

# 作業記録 [9/10(1)]

### 1. 調査した内容
- バグ：トップ画面で都道府県を指定して施設一覧へ遷移した後,施設詳細画面や新規登録画面に移動してから「← 施設一覧に戻る」を押すと,フィルターが解除されて全件一覧ページ（/facilities）に戻ってしまう
- 原因：各画面への遷移時にurlのクエリパラメータ（prefectureId）を引き継いでおらず,各コンポーネントsearchParamsからprefectureIdを取得できていなかった.

### 2. 悩み・疑問
- 施設詳細画面側でbackUrlの条件分岐を記述していたものの,一覧画面からのリンクに?prefectureId=XXが付与されていなかったため,常にundefinedと判定されて全件一覧へ遷移していた.

### 3. 学習した内容
- 自分は画面遷移する際は移動したいページのpathを渡せばいいと考えていたのですが,今回のように条件を絞り込んだ後の状態のページへリダイレクトする際は前の段階でキーとなる値を受け渡す必要があることを学習した.
- 

### 4. 設計
- 画面間データ共有のフロー:
    - トップ画面：router.push('/facilities?prefectureId=XX')
    - 施設一覧画面：searchParamsからprefectureIdを取得し,詳細画面および新規作成画面の<Link>に?prefectureId={prefectureId}を動的付与.
    - 施設詳細画面：searchParamsからprefectureIdを取得し、戻り先（backUrlを/facilities?prefectureId=${prefectureId}に設定.
    - 新規施設登録画面：同様にprefectureIdを引き継ぎ、戻り先およびフォームの都道府県初期値に反映.

### 5. 仕様
- 各種登録フォーム欄の任意部分の項目を追加
- 施設一覧表示画面と施設詳細画面のファイルをそれぞれprefectureIdを受け渡して画面遷移できるように変更した

## 作業記録 [9/10(2)]
- 対象機能: 施設詳細（/facilities/[id]）、施設編集（/facilities/[id]/edit）
### 1. 調査した内容
- 登録済みの施設情報に対しての編集,削除機能の追加
    - 削除時に紐づく訪問ログまで同時に消えてしまうと誤操作時の被害が大きいため,訪問ログが残っているのもは削除できないようにした

### 2. 悩み・疑問
- 課題として,まだバリデーション機能を考えれていないため,今後取り組む予定だが,削除可能施設の絞り込みや,編集不可の欄の条件定義や編集する際のバリデーションなども考えたい

### 3. 学習した内容
- 子テーブルで参照されている親テーブルの情報は削除時に使用上でのエラーが発生すると考えていたが,削除機能として単純にdelete()では子テーブルの情報まで削除してしまったため,そのミスを避けるための条件について考えることの重要性を学びました


### 4. 設計
- 編集機能：
    - DBから対象施設データを取得し、フォームのdefaultValueに初期表示
    - prefectureId を引き継いで詳細画面へリダイレクト
- 削除機能：
    - サーバー側で対象施設のvisitCountを計測し,1件以上存在する場合は処理を中断,UI側でもdisabled={facility.visits.length > 0} とし、ボタンを非活性化して誤操作を防ぐ

### 5. 仕様
- src/app/facilities/[id]/page.tsx
    - 「編集」ボタンの設置
    - 「削除」用 Server Action の追加
    - 訪問ログが存在する場合は削除ボタンを非活性化する

- src/app/facilities/[id]/edit/page.tsx
    - 既存データ読み込み付き編集フォームの実装

## 作業記録 [9/11(1)]

### 1. 調査した内容
- ログイン時のアカウント作成方法について
    - 今回はreactフックを使い名前とemailアドレスが一致している場合承認する仕様にした
    - 他の方法としてcookie()を用いる方法もある
    - -> 今回はテスト作成のため自分しか使用しない点と,研修の復習という点から一つ目の方法を採用した
- フォルダ構成について：
    - Next.js15におけるフォルダ階層とURLパスの対応ルール

### 2. 悩み・疑問
- フォルダ階層とurlパスの対応について元々,(/top/page.tsx)をトップ画面に設定しており,アプリ実行時(pnpm dev)に表示されていたものをログイン後に表示されるようにしたかったため,フォルダ構成を変えようとしたら,aiが「userIdを受け取らなかった場合ログイン画面に飛ぶようにすればいい」と言われたため修正しようとしたができなかったので断念した.
    - その方法で本当にできるのか,なぜできなかったのかが疑問でした.

### 3. 学習した内容
- cookie()について
- stateで管理した場合の処理の流れについて

### 4. 設計
- フォルダ構成の変更：
    - ログイン完了後にトップページに移動できるようにした

### 5. 仕様
- userIdを受け渡すようにしたため,この後に自分が登録したログ情報のみを編集可能にする機能を追加する予定
- 現在はログイン画面からtopページまでしかuserIdを受け渡しを行っていないため,施設登録とログ追加機能はランダムで登録されたユーザーを選択し,ログを残す仕様になっている

## 作業記録 [9/11(2)]

### 1. 調査した内容
- URLクエリパラメータ(userId, prefectureId)の正しい取得,保持パターンの調査
- 訪問ログの編集,キャンセル,完了時に存在しないパス（/visits）へ遷移し404エラーが発生する問題の原因調査および正しいリダイレクト先（/facilities/[id]）の整理

### 2. 悩み・疑問
- 未ログイン状態(userIdなし)や第三者による直リンク,直叩き操作に対する,安全なサーバーサイドのアクセス制限方法
- 

### 3. 学習した内容
- ヘルパー関数：特定のめんどくさい処理を一つにまとめてつでも呼び出せるようにした関数
- URLSearchParams()：&や=を手打ちしなくても移動で正しいurlの形に整形してくれる
- query.set()：URLSearchParamsオブジェクトに新しいパラメーターを追加するための関数
    - 設定する値は文字列の必要がある　-> URLSearchParamsがurlテキストを扱うため
    - set()のおかげで重複を防いでくれる
- Object.entries()：オブジェクトを[key,value]の二次元配列に変換する関数
    - オブジェクトはループ処理が不可能なため,一度配列に直して,forEachで展開する必要がある

### 4. 設計
- クエリ維持ヘルパー(buildQuery): 任意,必須のクエリパラメータを保持し,末尾の ?,&などのURL文字列を自動生成する関数を使用
- 

### 5. 仕様
- 訪問ログ編集機能: 本人のみが編集画面(/facilities/[id]/visits/[visitId]/edit)にアクセスでき,コメントの更新が可能
- 画面遷移の統一: 編集の完了,キャンセル,訪問ログの新規登録完了(VisitSuccessPage)後は,すべてuserIdとprefectureIdを引き継いだ状態で該当の施設詳細画面(/facilities/[id])へ遷移
- 新規施設登録からの画面遷移を変更していないためuserIdの受け渡しが保持されていない

## 作業記録 [9/14(1)]

### 1. 調査した内容
- ページ遷移時にuserId,prefectureIdの情報を保持させるために,まだ修正できてない箇所を調査

### 2. 悩み・疑問
- 画面遷移する箇所の最適化
- バリデーション機能の追加

### 3. 学習した内容
- 二重チェックの重要性：
    - ui上のrequire属性のみでのバリデーション機能だけではなく,サーバー側でも未然に必須項目のデータが送られてこなかった場合,処理を中断する機能を組み込むことでui側でもサーバー側でもエラーを防げることを学習しました.
- searchparamsがどうやってurlの?以降の文字列を取得してきているのかについて
    - ->const searchParams = await props.searchParams

### 4. 設計
- 登録完了画面のuiのデザインの変更

### 5. 仕様
- 新規施設登録時および訪問ログ編集時にuserId,prefectureIdを保持したまま遷移するように変更
- 新規施設登録完了画面を経由する際にfacilityId,prefectureId情報を保持したまま移動可能に変更

## 作業記録 [9/15(1)]

### 1. 調査した内容
- テストケースで見つけた意図しない挙動の確認
    - 施設編集画面から完了画面に遷移する際に,userIdをクエリパラメーターで保持する仕様に変更
    - 新規訪問ログ追加画面の画像url入力欄を,自身のpc内の画像ファイルを登録できるように変更
- 以前のテストケースから変更を加えたベー時に対応させるために,テストケース自体の変更

### 2. 悩み・疑問
- すでに追加されている画像ファイルを編集する機能を時間の関係で断念

### 3. 学習した内容
- 必須項目をhtmlだけでなく,サーバー側のバリデーション機能が効いているか確認するために,ブラウザ上の管理者権限から属性を変更させるやり方

- 指定したファイルを指定フォルダにダウンロードするやり方
    - 前提：webブラウザ上で動くjsはPc内のファイルを参照することはできない
    - -> ユーザーが明示的に操作した時のみファイルへのアクセス権限をjsに渡す(ブラウザの仕組みにより)
    - fsモジュール：ファイルやフォルダの場所を示す
    - pathモジュール：ファイルやフォルダを操作する

- 登録する画像の保存先をpublicにするのではなく,uploadsフォルダにするべき理由について
    - webサイトのアイコン画像などシステム上必要な画像もあるため混在してしまう
    - 開発途中の場合まとめて消すこともできる
    - 保守性の面から.gitignoreに指定することもできる

### 4. 設計
- 画像アップロード用のフォルダの作成
- 訪問ログ編集画面にコメント欄だけでなく,日時,評価,利用料金欄の追加

### 5. 仕様
- すべてのページでuserId情報を保持させる
- userIdを保持せずに画面遷移する場合ログイン画面にリダイレクトする